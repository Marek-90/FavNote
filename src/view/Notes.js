import UserPageTemplate from "../templates/UserPageTemplate";

const Notes = () => {
  return (
    <UserPageTemplate>
      <h1>Notes view</h1>
    </UserPageTemplate>
  );
};

export default Notes;
